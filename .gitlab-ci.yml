image: node:22

stages:
  - install
  - build
  - deploy

cache: {}

variables:
  NODE_VERSION: "22"

before_script:
  - rm -rf package-lock.json node_modules

install_dependencies:
  stage: install
  script:
    - npm i --legacy-peer-deps
  artifacts:
    paths:
      - node_modules/
      - local_modules/

build_project:
  stage: build
  script:
    - npm run build
  artifacts:
    paths:
      - dist/
  only:
    - main

pages:
  stage: deploy
  dependencies:
    - build_project
  script:
    - mkdir -p public
    - cp -r dist/* public/
  artifacts:
    paths:
      - public
  only:
    - main
